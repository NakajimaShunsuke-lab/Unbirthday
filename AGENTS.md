# AGENTS.md - Unbirthday

Codex should keep this file small. Use it as an index to focused agent guides instead of adding long instructions here.

## Required Reads

- Component work: read [docs/agent-guides/components.md](docs/agent-guides/components.md) before creating, editing, or refactoring UI components.

## Project Basics

- Stack: React 19, Vite, Tailwind CSS 4.
- Source files live in `src/`.
- Global styles live in `src/styles.css`.
- Visual direction lives in `DESIGN.md`; if it is unclear or conflicts with actual code, prefer the current app behavior and mention the ambiguity.

## Maintenance Rule

When a new recurring Codex instruction is needed, add a short link here and put the full guidance in `docs/agent-guides/`.

